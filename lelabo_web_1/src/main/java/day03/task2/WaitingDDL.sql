drop database if exists Waiting0116;
create database Waiting0116;
use Waiting0116;
create table Waiting(
   num int auto_increment ,
    phone longtext , 
    count int ,
    constraint primary key(num)
);

